use mrk::models;
    
namespace mrk {
    namespace web {
        class HttpMethod {
            static var<int> GET = 0;
            static var<int> POST = 1;
        }
        
        class HttpResponse {
            var<string> body;
            var<int> code;
        }

        class Http {
            __declspec(NATIVE) static func request(string url, int method) -> HttpResponse {
            }
        }
    }
}